services:
  - type: web
    name: wanderlust
    runtime: node
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: ATLAS_DB
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production